<div class="container">
  <h4> Users </h4>
  <%= turbo_stream_from "users" %>
  <div id="users">
    <%= render @users %>
  </div>
  <h4> Rooms </h4>
  <%= render partial: "layouts/new_room_form" %>
  <%= turbo_stream_from "rooms" %>
  <div id="rooms">
    <%= render @rooms %>
  </div>
</div>
