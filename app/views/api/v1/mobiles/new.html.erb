<style>
  .container{
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
</style>

<%= form_with url: api_v1_mobiles_path do |f| %>
  <%= f.text_field :country_code %>
  <%= f.number_field :phone_number %>
  <%= f.submit %>
<% end %>

<!-- <script>
  function getById(id){
    return document.getElementById(id)
  }

  const sendOTP = getById('send-otp');
  const mobile = getById('mobile')
  const message = getById('message')

  const submitOtp = getById('submit-otp')
  const otp = getById('otp')

  sendOTP.addEventListener('click', function () {
    if(mobile.value.length == 10){
      setOtpEnterContainer()
    }
    else{
      message.innerHTML = "must be 10 digit"
    }
  });

  submitOtp.addEventListener('click', function() {
    if (otp.value.length == 6) {
      message.innerHTML = ""

    }
    else{
      message.innerHTML = "invalid"
    }
  });

  function setOtpEnterContainer(){
    const enterNumberContainer = getById('enter-number-container')
    const submitOtpContainer = getById('submit-otp-container')
    const constMobile = getById('const-mobile')
    constMobile.innerHTML = mobile.value
    enterNumberContainer.style.visibility = "hidden"
    enterNumberContainer.style.height = "0px"
    submitOtpContainer.style.visibility = "visible"
  }


</script> -->

